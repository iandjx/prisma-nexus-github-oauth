# Migration `20200525205656`

This migration has been generated by Ian de Jesus at 5/25/2020, 8:56:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_User" (
    "bio" TEXT   ,
    "githubUserId" TEXT NOT NULL  ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "name" TEXT   ,
    "public_gists" INTEGER NOT NULL  ,
    "public_repos" INTEGER NOT NULL  
) 

INSERT INTO "quaint"."new_User" ("id", "name") SELECT "id", "name" FROM "quaint"."User"

DROP TABLE "quaint"."User";

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User.githubUserId" ON "User"("githubUserId")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

DROP TABLE "quaint"."Profile";

DROP TABLE "quaint"."Post";

DROP TABLE "quaint"."new_User";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200525204939..20200525205656
--- datamodel.dml
+++ datamodel.dml
@@ -1,27 +1,17 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:./dev.db"
 }
 generator client {
   provider = "prisma-client-js"
 }
-model Post {
-  id        Int     @default(autoincrement()) @id
-  title     String
-  content   String?
-  published Boolean @default(false)
-  authorId  Int?
-  author    User?   @relation(fields: [authorId], references: [id])
-}
-
 model User {
   id           Int     @default(autoincrement()) @id
   name         String?
   githubUserId String  @unique
   bio          String?
   public_repos Int
   public_gists Int
-  Post         Post[]
 }
```


